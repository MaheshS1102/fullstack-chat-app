{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run start --prefix backend",
    "build": "npm install --prefix frontend && npm install --prefix frontend && npm run build --prefix frontend"
  },
  "type": "commonjs"
}
